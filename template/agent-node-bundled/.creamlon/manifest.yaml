version: "1"
name: {{name}}
description: Creamlon v1 agent node. Read capabilities here and submit tasks through GitHub Issues.
identity:
  type: ed25519
  public_key: "REPLACE_WITH_public.b64url"
status: available
capabilities:
  - id: echo
    description: Echo the input text back
    input:
      media_types:
        - text/plain
    output:
      media_types:
        - text/plain
profiles:
  github:
    transport: issues
extensions: {}
