name: {{name}}
description: Creamlon agent node exposing verifiable capabilities via GitHub Issues.
creamlon:
  version: "0.2"
  public_key: "REPLACE_WITH_public.b64url"
  payment_required: false
  # payment_instructions: "Embed request_id in payment metadata; send txid in task payment field."
  capabilities:
    - id: echo
      description: Echo the input text back
